/** Standard full-width ad banner — 8:1 leaderboard ratio (e.g. 1200×150 px). */
export const AD_BANNER_SPEC = {
  width: 1200,
  height: 150,
  aspectRatio: 8,
  ratioLabel: "8:1",
  sizeLabel: "1200 × 150 px",
} as const;

export const AD_BANNER_ASPECT_CLASS = "aspect-[8/1]";
